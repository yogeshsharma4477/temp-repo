//we can directly call this file without exporting, just 
//require('./7-mind-granede') , you need invoke function in that file 

function add(){
    console.log(`addition of ${2+2}`);
}
console.log(add());